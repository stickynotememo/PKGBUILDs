# PKGBUILDs

PKGBUILDS for packages [I maintain or co-maintain](https://aur.archlinux.org/packages?K=stickynotememo&SeB=M) on the AUR.

Clone with `git clone --recurse-submodules` to download PKGBUILDs from the AUR.
